"use client";

import { ChangeEvent, FormEvent, useEffect, useState } from "react";

type Settings = { companyName: string; logoName: string; phone: string; email: string; address: string; apiKey: string; model: string; aiLanguage: string; replyStyle: string; whatsappEnabled: boolean; businessNumber: string; webhook: string; messengerEnabled: boolean; newLeadNotice: boolean; appointmentNotice: boolean; timezone: string; language: string; currency: string; dateFormat: string };
const storageKey = "propertyai-settings";
const defaults: Settings = { companyName: "PropertyAI 房地产", logoName: "", phone: "+60 3 1234 5678", email: "hello@propertyai.my", address: "Kuala Lumpur, Malaysia", apiKey: "", model: "默认模型", aiLanguage: "自动识别客户语言", replyStyle: "专业", whatsappEnabled: false, businessNumber: "", webhook: "", messengerEnabled: false, newLeadNotice: true, appointmentNotice: true, timezone: "Asia/Kuala_Lumpur", language: "简体中文", currency: "RM", dateFormat: "YYYY-MM-DD" };

function Toggle({ enabled, onChange }: { enabled: boolean; onChange: () => void }) { return <button type="button" onClick={onChange} className={`relative h-6 w-11 rounded-full transition ${enabled ? "bg-[#315bd4]" : "bg-slate-200"}`}><span className={`absolute top-1 h-4 w-4 rounded-full bg-white transition ${enabled ? "left-6" : "left-1"}`} /></button>; }
export default function SettingsPage() { const [settings, setSettings] = useState(defaults); const [notice, setNotice] = useState(""); useEffect(() => { try { const saved = window.localStorage.getItem(storageKey); if (saved) setSettings({ ...defaults, ...JSON.parse(saved) }); } catch { window.localStorage.removeItem(storageKey); } }, []); function update<K extends keyof Settings>(key: K, value: Settings[K]) { setSettings((current) => ({ ...current, [key]: value })); } function save(event: FormEvent<HTMLFormElement>) { event.preventDefault(); window.localStorage.setItem(storageKey, JSON.stringify(settings)); setNotice("设置已保存"); window.setTimeout(() => setNotice(""), 2400); } function uploadLogo(event: ChangeEvent<HTMLInputElement>) { const file = event.target.files?.[0]; if (file) update("logoName", file.name); } const input = "mt-2 h-10 w-full rounded-lg border border-slate-200 px-3 text-sm outline-none focus:border-[#315bd4]"; return <main className="min-h-screen px-6 py-20 text-[#172033] sm:px-10"><form onSubmit={save} className="mx-auto max-w-5xl"><div className="flex flex-wrap items-end justify-between gap-4"><div><p className="text-sm font-semibold text-[#315bd4]">账户与系统</p><h1 className="mt-2 text-3xl font-semibold tracking-[-0.04em]">系统设置</h1><p className="mt-2 text-slate-600">配置公司资料、AI 助理和通知偏好。</p></div><button type="submit" className="rounded-xl bg-[#315bd4] px-4 py-2.5 text-sm font-semibold text-white shadow-lg shadow-blue-500/20">保存全部设置</button></div><div className="mt-8 grid gap-6 lg:grid-cols-2"><section className="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><h2 className="font-semibold">公司资料</h2><div className="mt-5 space-y-4"><label className="text-sm font-medium">公司名称<input value={settings.companyName} onChange={(event) => update("companyName", event.target.value)} className={input}/></label><label className="block text-sm font-medium">Logo 上传<input type="file" accept="image/*" onChange={uploadLogo} className="mt-2 block w-full text-sm"/>{settings.logoName && <p className="mt-2 text-xs text-slate-500">已选择：{settings.logoName}</p>}</label><label className="text-sm font-medium">电话<input value={settings.phone} onChange={(event) => update("phone", event.target.value)} className={input}/></label><label className="text-sm font-medium">Email<input type="email" value={settings.email} onChange={(event) => update("email", event.target.value)} className={input}/></label><label className="text-sm font-medium">地址<input value={settings.address} onChange={(event) => update("address", event.target.value)} className={input}/></label></div></section><section className="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><h2 className="font-semibold">AI 设置</h2><div className="mt-5 space-y-4"><label className="text-sm font-medium">OpenAI API Key<input type="password" value={settings.apiKey} onChange={(event) => update("apiKey", event.target.value)} placeholder="仅保存于当前浏览器" className={input}/></label><label className="text-sm font-medium">默认模型<select value={settings.model} onChange={(event) => update("model", event.target.value)} className={input}><option>默认模型</option><option>gpt-4.1-mini</option><option>预留扩展</option></select></label><label className="text-sm font-medium">AI 回复语言<select value={settings.aiLanguage} onChange={(event) => update("aiLanguage", event.target.value)} className={input}><option>自动识别客户语言</option><option>简体中文</option><option>English</option><option>Bahasa Melayu</option></select></label><label className="text-sm font-medium">回复风格<select value={settings.replyStyle} onChange={(event) => update("replyStyle", event.target.value)} className={input}><option>专业</option><option>友好</option><option>简洁</option></select></label></div></section><section className="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><h2 className="font-semibold">WhatsApp 设置</h2><div className="mt-5 space-y-4"><div className="flex items-center justify-between"><div><p className="text-sm font-medium">启用 WhatsApp</p><p className="text-xs text-slate-500">连接 Business API 后启用。</p></div><Toggle enabled={settings.whatsappEnabled} onChange={() => update("whatsappEnabled", !settings.whatsappEnabled)}/></div><label className="text-sm font-medium">Business Number<input value={settings.businessNumber} onChange={(event) => update("businessNumber", event.target.value)} className={input}/></label><label className="text-sm font-medium">Webhook（预留）<input value={settings.webhook} onChange={(event) => update("webhook", event.target.value)} placeholder="https://" className={input}/></label><div className="border-t border-slate-100 pt-4"><div className="flex items-center justify-between"><div><p className="text-sm font-medium">Facebook Messenger（预留）</p><p className="text-xs text-slate-500">未来可连接 Messenger 渠道。</p></div><Toggle enabled={settings.messengerEnabled} onChange={() => update("messengerEnabled", !settings.messengerEnabled)}/></div></div></div></section><section className="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><h2 className="font-semibold">通知与系统</h2><div className="mt-5 space-y-5"><div className="flex items-center justify-between"><span className="text-sm font-medium">新客户通知</span><Toggle enabled={settings.newLeadNotice} onChange={() => update("newLeadNotice", !settings.newLeadNotice)}/></div><div className="flex items-center justify-between"><span className="text-sm font-medium">新预约通知</span><Toggle enabled={settings.appointmentNotice} onChange={() => update("appointmentNotice", !settings.appointmentNotice)}/></div><label className="block text-sm font-medium">时区<select value={settings.timezone} onChange={(event) => update("timezone", event.target.value)} className={input}><option>Asia/Kuala_Lumpur</option><option>Asia/Shanghai</option></select></label><label className="block text-sm font-medium">语言<select value={settings.language} onChange={(event) => update("language", event.target.value)} className={input}><option>简体中文</option><option>English</option></select></label><label className="block text-sm font-medium">币种<select value={settings.currency} onChange={(event) => update("currency", event.target.value)} className={input}><option>RM</option></select></label><label className="block text-sm font-medium">日期格式<select value={settings.dateFormat} onChange={(event) => update("dateFormat", event.target.value)} className={input}><option>YYYY-MM-DD</option><option>DD/MM/YYYY</option></select></label></div></section></div></form>{notice && <div role="status" className="fixed bottom-5 right-5 z-[80] rounded-xl bg-[#172b57] px-4 py-3 text-sm font-semibold text-white">✓ {notice}</div>}</main>; }
