"use client";

import { useEffect, useState } from "react";
import type { Appointment } from "./leads/[slug]/appointments";

const fallback = [{ id: "demo-1", customerName: "王先生", property: "Arcoris Mont Kiara", date: "2026-07-25", time: "15:00", status: "已确认" }, { id: "demo-2", customerName: "Ahmad", property: "Desa ParkCity South Brooks", date: "2026-07-26", time: "14:00", status: "待确认" }];
export default function DashboardAppointments() { const [appointments, setAppointments] = useState(fallback); const [knowledgeCount, setKnowledgeCount] = useState(15); useEffect(() => { try { const savedAppointments = window.localStorage.getItem("propertyai-appointments"); if (savedAppointments) { const parsed = JSON.parse(savedAppointments) as Appointment[]; if (parsed.length) setAppointments(parsed); } const savedKnowledge = window.localStorage.getItem("propertyai-knowledge"); if (savedKnowledge) { const parsedKnowledge = JSON.parse(savedKnowledge) as unknown[]; setKnowledgeCount(parsedKnowledge.length); } } catch { return; } }, []); return <><section className="mt-6 grid gap-4 sm:grid-cols-2"><article className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><p className="text-sm text-slate-500">今日预约看房</p><div className="mt-3 flex items-end justify-between"><strong className="text-3xl">{appointments.filter((item) => item.status !== "已取消").length}</strong><span className="text-xs font-semibold text-[#315bd4]">预约行程</span></div></article><article className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><p className="text-sm text-slate-500">Knowledge Files</p><div className="mt-3 flex items-end justify-between"><strong className="text-3xl">{knowledgeCount}</strong><span className="text-xs font-semibold text-emerald-600">AI 知识库</span></div></article></section><section className="mt-6 rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><div className="flex items-center justify-between"><div><h2 className="font-semibold">最近预约</h2><p className="mt-1 text-sm text-slate-500">客户、房源与看房状态一览。</p></div></div><div className="mt-4 divide-y divide-slate-100">{appointments.slice(0, 4).map((item) => <div key={item.id} className="flex flex-wrap items-center justify-between gap-2 py-3 text-sm"><div><p className="font-medium">{item.customerName} · {item.property}</p><p className="mt-1 text-slate-500">{item.date} {item.time}</p></div><span className="rounded-full bg-[#eaf0ff] px-2.5 py-1 text-xs font-semibold text-[#315bd4]">{item.status}</span></div>)}</div></section></>; }
