"use client";

import { useEffect, useState } from "react";

export default function SystemStatus() { const [knowledge, setKnowledge] = useState(4); const [appointments, setAppointments] = useState(1); const [whatsapp, setWhatsapp] = useState(false); useEffect(() => { try { const savedKnowledge = window.localStorage.getItem("propertyai-knowledge"); if (savedKnowledge) setKnowledge((JSON.parse(savedKnowledge) as unknown[]).length); const savedAppointments = window.localStorage.getItem("propertyai-appointments"); if (savedAppointments) setAppointments((JSON.parse(savedAppointments) as unknown[]).length); const savedSettings = window.localStorage.getItem("propertyai-settings"); if (savedSettings) setWhatsapp(Boolean(JSON.parse(savedSettings).whatsappEnabled)); } catch { return; } }, []); const items = [["AI", "未连接"], ["WhatsApp", whatsapp ? "已启用" : "未连接"], ["Knowledge", `${knowledge} 个文件`], ["Customers", "4"], ["Properties", "4"], ["Appointments", `${appointments}`]]; return <section className="mt-6 rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><p className="text-sm font-semibold">系统状态</p><div className="mt-4 grid gap-3 sm:grid-cols-3 lg:grid-cols-6">{items.map(([label, value]) => <div key={label} className="rounded-xl bg-slate-50 p-3"><p className="text-xs text-slate-400">{label}</p><p className="mt-1 text-sm font-semibold">{value}</p></div>)}</div></section>; }
